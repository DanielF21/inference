"""Benchmark prompts, sliced to exact token counts.

Generated by scripts/build_prompts.py from scripts/prompt_source.txt.
Edit those, not this file.
"""

PROMPTS = {
    'p16': (
        'Language model inference is the process of turning a trained set of weights into '
        'generated text'
    ),
    'p64': (
        'Language model inference is the process of turning a trained set of weights into '
        'generated text, one token at a time. It looks simple from the outside: text goes in, '
        'text comes out. Inside, it is a tightly constrained systems problem where the dominant '
        'costs are rarely the ones a newcomer expects. The floating point operations are'
    ),
    'p256': (
        'Language model inference is the process of turning a trained set of weights into '
        'generated text, one token at a time. It looks simple from the outside: text goes in, '
        'text comes out. Inside, it is a tightly constrained systems problem where the dominant '
        'costs are rarely the ones a newcomer expects. The floating point operations are cheap. '
        'Moving bytes is expensive. Almost every technique that separates a fast serving engine '
        'from a slow one is, at bottom, a way of moving fewer bytes or of amortizing a fixed cost '
        'across more useful work.\n\nA decoder only transformer generates text autoregressively. '
        'Given a sequence of tokens, it produces a probability distribution over the vocabulary '
        'for the next position. A sampling rule turns that distribution into a single token '
        'identifier, the token is appended to the sequence, and the process repeats. Nothing '
        'about this loop is parallel across time. Token five cannot be computed until token four '
        'exists, because token four is part of the input that produces token five. This '
        'sequential dependency is the central fact of inference, and it is what makes generation '
        'fundamentally different from training, where an entire sequence can be processed in one '
        'shot with teacher forcing.\n\nThe generation loop splits naturally into two phases with '
        'completely different performance characteristics. The first phase, prefill, processes '
        'the entire input prompt in'
    ),
    'p1024': (
        'Language model inference is the process of turning a trained set of weights into '
        'generated text, one token at a time. It looks simple from the outside: text goes in, '
        'text comes out. Inside, it is a tightly constrained systems problem where the dominant '
        'costs are rarely the ones a newcomer expects. The floating point operations are cheap. '
        'Moving bytes is expensive. Almost every technique that separates a fast serving engine '
        'from a slow one is, at bottom, a way of moving fewer bytes or of amortizing a fixed cost '
        'across more useful work.\n\nA decoder only transformer generates text autoregressively. '
        'Given a sequence of tokens, it produces a probability distribution over the vocabulary '
        'for the next position. A sampling rule turns that distribution into a single token '
        'identifier, the token is appended to the sequence, and the process repeats. Nothing '
        'about this loop is parallel across time. Token five cannot be computed until token four '
        'exists, because token four is part of the input that produces token five. This '
        'sequential dependency is the central fact of inference, and it is what makes generation '
        'fundamentally different from training, where an entire sequence can be processed in one '
        'shot with teacher forcing.\n\nThe generation loop splits naturally into two phases with '
        'completely different performance characteristics. The first phase, prefill, processes '
        'the entire input prompt in a single forward pass. Every position attends to every '
        'earlier position, all of the matrix multiplications are large, and the hardware is kept '
        'busy. Prefill is compute bound: the arithmetic units are the limiting resource, and the '
        'time it takes scales with the length of the prompt. The second phase, decode, produces '
        'the remaining tokens one at a time. Each decode step processes exactly one new position. '
        'The matrix multiplications degenerate into matrix vector products, the arithmetic units '
        'sit mostly idle, and the limiting resource becomes the rate at which weights can be '
        'pulled out of memory.\n\nUnderstanding why decode is memory bound requires only one '
        'calculation. At batch size one, generating a single token requires reading every weight '
        'in the model exactly once. A model with one and a half billion parameters stored in '
        'sixteen bit precision occupies roughly three gigabytes. On an accelerator with six '
        'hundred gigabytes per second of memory bandwidth, reading three gigabytes takes about '
        'five milliseconds. That is the floor on time per token, regardless of how fast the '
        'arithmetic units are, because the arithmetic cannot begin until the weights arrive. The '
        'useful work performed during those five milliseconds is about three billion floating '
        'point operations, which a modern accelerator could complete in well under a tenth of a '
        'millisecond. The hardware is idle more than ninety eight percent of the time.\n\nThis '
        'ratio has a name. Arithmetic intensity is the number of floating point operations '
        'performed per byte moved from memory. Every accelerator has a characteristic ratio of '
        'peak compute to peak bandwidth, sometimes called the ridge point or machine balance. '
        'Workloads with arithmetic intensity below the ridge point are memory bound and cannot '
        'reach peak compute no matter how well written the kernels are. Workloads above it are '
        'compute bound. Plotting achievable performance against arithmetic intensity produces the '
        'roofline model, a diagram with a sloped bandwidth limited region on the left and a flat '
        'compute limited ceiling on the right. Single stream decode sits far down the sloped '
        'portion, which is precisely why the first and largest optimization available is to '
        'increase arithmetic intensity rather than to write faster kernels.\n\nThe most direct way '
        'to raise arithmetic intensity is batching. If many independent sequences are decoded '
        'simultaneously, the weights are read once and used for every sequence in the batch. The '
        'bytes moved stay roughly constant while the useful work multiplies by the batch size. '
        'Arithmetic intensity therefore rises approximately linearly with batch size, and '
        'throughput rises with it until the ridge point is reached and the workload becomes '
        'compute bound. This is why serving systems care so intensely about keeping batches full, '
        'and why a system that decodes one request at a time is leaving an order of magnitude of '
        'throughput unclaimed even with perfectly optimized kernels.\n\nBefore batching can help, '
        'though, a more basic inefficiency has to be removed. A naive generation loop feeds the '
        'entire growing sequence back through the model at every step. Step one processes the '
        'prompt. Step two reprocesses the prompt plus one token. Step three reprocesses the '
        'prompt plus two tokens, and so on. The total work grows with the square of the number of '
        'generated tokens, and almost all of it is recomputation of values that were already '
        'computed and then discarded. The fix is the key value cache, and it is the single '
        'largest algorithmic win available in transformer inference.\n\nThe insight behind the '
        'cache comes from the structure of causal attention. When a new token attends to the '
        'sequence, it computes a query vector for itself and compares that query against key '
        'vectors from every position, then takes a weighted combination of value vectors from '
        'every position. The key and value vectors for earlier positions depend only on those '
        'earlier positions. Because the attention mask is causal, no earlier position ever '
        'attends forward to a later one, which means adding a new token cannot change any earlier '
        'key or value. They are invariant. Recomputing them is pure waste. Storing them and '
        'reusing them turns each'
    ),
    'p4096': (
        'Language model inference is the process of turning a trained set of weights into '
        'generated text, one token at a time. It looks simple from the outside: text goes in, '
        'text comes out. Inside, it is a tightly constrained systems problem where the dominant '
        'costs are rarely the ones a newcomer expects. The floating point operations are cheap. '
        'Moving bytes is expensive. Almost every technique that separates a fast serving engine '
        'from a slow one is, at bottom, a way of moving fewer bytes or of amortizing a fixed cost '
        'across more useful work.\n\nA decoder only transformer generates text autoregressively. '
        'Given a sequence of tokens, it produces a probability distribution over the vocabulary '
        'for the next position. A sampling rule turns that distribution into a single token '
        'identifier, the token is appended to the sequence, and the process repeats. Nothing '
        'about this loop is parallel across time. Token five cannot be computed until token four '
        'exists, because token four is part of the input that produces token five. This '
        'sequential dependency is the central fact of inference, and it is what makes generation '
        'fundamentally different from training, where an entire sequence can be processed in one '
        'shot with teacher forcing.\n\nThe generation loop splits naturally into two phases with '
        'completely different performance characteristics. The first phase, prefill, processes '
        'the entire input prompt in a single forward pass. Every position attends to every '
        'earlier position, all of the matrix multiplications are large, and the hardware is kept '
        'busy. Prefill is compute bound: the arithmetic units are the limiting resource, and the '
        'time it takes scales with the length of the prompt. The second phase, decode, produces '
        'the remaining tokens one at a time. Each decode step processes exactly one new position. '
        'The matrix multiplications degenerate into matrix vector products, the arithmetic units '
        'sit mostly idle, and the limiting resource becomes the rate at which weights can be '
        'pulled out of memory.\n\nUnderstanding why decode is memory bound requires only one '
        'calculation. At batch size one, generating a single token requires reading every weight '
        'in the model exactly once. A model with one and a half billion parameters stored in '
        'sixteen bit precision occupies roughly three gigabytes. On an accelerator with six '
        'hundred gigabytes per second of memory bandwidth, reading three gigabytes takes about '
        'five milliseconds. That is the floor on time per token, regardless of how fast the '
        'arithmetic units are, because the arithmetic cannot begin until the weights arrive. The '
        'useful work performed during those five milliseconds is about three billion floating '
        'point operations, which a modern accelerator could complete in well under a tenth of a '
        'millisecond. The hardware is idle more than ninety eight percent of the time.\n\nThis '
        'ratio has a name. Arithmetic intensity is the number of floating point operations '
        'performed per byte moved from memory. Every accelerator has a characteristic ratio of '
        'peak compute to peak bandwidth, sometimes called the ridge point or machine balance. '
        'Workloads with arithmetic intensity below the ridge point are memory bound and cannot '
        'reach peak compute no matter how well written the kernels are. Workloads above it are '
        'compute bound. Plotting achievable performance against arithmetic intensity produces the '
        'roofline model, a diagram with a sloped bandwidth limited region on the left and a flat '
        'compute limited ceiling on the right. Single stream decode sits far down the sloped '
        'portion, which is precisely why the first and largest optimization available is to '
        'increase arithmetic intensity rather than to write faster kernels.\n\nThe most direct way '
        'to raise arithmetic intensity is batching. If many independent sequences are decoded '
        'simultaneously, the weights are read once and used for every sequence in the batch. The '
        'bytes moved stay roughly constant while the useful work multiplies by the batch size. '
        'Arithmetic intensity therefore rises approximately linearly with batch size, and '
        'throughput rises with it until the ridge point is reached and the workload becomes '
        'compute bound. This is why serving systems care so intensely about keeping batches full, '
        'and why a system that decodes one request at a time is leaving an order of magnitude of '
        'throughput unclaimed even with perfectly optimized kernels.\n\nBefore batching can help, '
        'though, a more basic inefficiency has to be removed. A naive generation loop feeds the '
        'entire growing sequence back through the model at every step. Step one processes the '
        'prompt. Step two reprocesses the prompt plus one token. Step three reprocesses the '
        'prompt plus two tokens, and so on. The total work grows with the square of the number of '
        'generated tokens, and almost all of it is recomputation of values that were already '
        'computed and then discarded. The fix is the key value cache, and it is the single '
        'largest algorithmic win available in transformer inference.\n\nThe insight behind the '
        'cache comes from the structure of causal attention. When a new token attends to the '
        'sequence, it computes a query vector for itself and compares that query against key '
        'vectors from every position, then takes a weighted combination of value vectors from '
        'every position. The key and value vectors for earlier positions depend only on those '
        'earlier positions. Because the attention mask is causal, no earlier position ever '
        'attends forward to a later one, which means adding a new token cannot change any earlier '
        'key or value. They are invariant. Recomputing them is pure waste. Storing them and '
        'reusing them turns each decode step from a full forward pass over the whole sequence '
        'into a forward pass over a single token, and the quadratic term disappears.\n\nThe cost of '
        'that optimization is memory. The cache must hold a key vector and a value vector for '
        'every layer, every attention head, and every position in the sequence. For a model with '
        'twenty eight layers, two key value heads, and a head dimension of one hundred twenty '
        'eight, storing both key and value in sixteen bit precision costs about twenty eight '
        'kilobytes for every single token. A two thousand token conversation therefore costs '
        'roughly fifty six megabytes of cache, and that cost is paid per concurrent sequence, not '
        'once. The cache is not a fixed overhead like the weights. It grows linearly with total '
        'tokens in flight, and it grows without bound as context lengths increase.\n\nAttention '
        'architecture directly controls that growth rate. In classical multi head attention every '
        'query head has its own key and value projections, so the cache scales with the full head '
        'count. Multi query attention takes the opposite extreme, sharing a single key value pair '
        'across every query head, which shrinks the cache dramatically but measurably degrades '
        'quality. Grouped query attention interpolates between them. Query heads are partitioned '
        'into a small number of groups, and every query head within a group shares one key value '
        'pair. The query heads remain distinct, so they still compute different attention '
        'patterns over the same shared keys, but the number of stored key value pairs drops by '
        'the group size. A model with twelve query heads and two key value heads pays one sixth '
        'the cache cost of the equivalent multi head model, at a quality cost small enough that '
        'essentially every recent open model has adopted the technique.\n\nOnce the cache exists, '
        'the question becomes how to allocate it. The simplest scheme reserves a contiguous block '
        'per sequence, sized for the maximum length that sequence might reach. This is easy to '
        'implement and wasteful in two distinct ways. Internal fragmentation appears because a '
        'sequence that stops after two hundred tokens still holds a reservation sized for two '
        'thousand. External fragmentation appears because freed regions of different sizes '
        'gradually interleave, so a large contiguous request can fail even when the total free '
        'memory would easily accommodate it. In a serving system where output lengths are unknown '
        'in advance and vary by more than an order of magnitude, these effects can waste the '
        'majority of the memory nominally devoted to cache.\n\nPaged attention borrows the solution '
        'from virtual memory in operating systems. The cache is divided into fixed size blocks, '
        'each holding the keys and values for a small number of tokens. A sequence is represented '
        'not as a contiguous region but as a block table, a list of pointers to physical blocks '
        'that may be scattered anywhere in the pool. Blocks are allocated on demand as a sequence '
        'grows, so a sequence never reserves space it has not used. Internal fragmentation is '
        'bounded by the block size, external fragmentation vanishes entirely because every block '
        'is interchangeable, and the number of sequences that can be held resident rises '
        'substantially. Blocks can also be shared between sequences that have a common prefix, '
        'which turns a memory management scheme into a redundancy elimination scheme for '
        'free.\n\nScheduling is the other half of the problem. Static batching groups a fixed set '
        'of requests, runs them together until all are finished, and only then admits new work. '
        'Because output lengths vary, a batch finishes at the pace of its slowest member, and '
        'every sequence that completed early leaves an idle slot for the remainder of the batch. '
        'Requests arriving during that window wait even though capacity exists. The result is low '
        'utilization and long tail latency, and both problems worsen as output length variance '
        'grows.\n\nContinuous batching, sometimes described as iteration level scheduling, '
        'restructures the loop. Instead of committing to a fixed batch, the scheduler makes a '
        'decision before every forward pass. Sequences that finished are evicted immediately and '
        'their memory returned to the pool. Waiting requests are admitted into the freed slots at '
        'the very next iteration rather than at the end of a batch. The batch composition changes '
        'continuously, and the accelerator is kept saturated with useful work as long as the '
        'queue is non empty. In workloads with realistic variation in output length this alone '
        'can multiply throughput several times over, without changing a single kernel.\n\nMixing '
        'prefill and decode in the same scheduler introduces a subtle tension. A prefill for a '
        'long prompt is a large compute bound operation that can occupy the accelerator for a '
        'substantial period. Any decode steps for sequences already in flight are stalled while '
        'it runs, which shows up to the user as a sudden spike in inter token latency. Chunked '
        'prefill addresses this by splitting a long prefill into smaller pieces and interleaving '
        'them with ongoing decode work, trading a modest increase in prefill completion time for '
        'a much smoother latency profile. Other systems go further and disaggregate the two '
        'phases onto separate hardware pools entirely, so that prefill and decode never '
        'contend.\n\nKernel level optimization matters too, though it is usually not the first '
        'thing to reach for. A straightforward attention implementation materializes the full '
        'matrix of attention scores, which has a size proportional to the square of the sequence '
        'length, writes it to memory, reads it back to apply the softmax, and reads it again to '
        'weight the values. For long sequences that intermediate matrix is far larger than the '
        'inputs and outputs combined, and the traffic to and from memory dominates the runtime. '
        'Flash attention restructures the computation to process the sequence in tiles small '
        'enough to fit in fast on chip memory, computing the softmax incrementally with a running '
        'normalization so that the full score matrix is never written out. The arithmetic is '
        'nearly identical; the memory traffic is drastically lower.\n\nQuantization attacks the '
        'byte count directly. If decode is limited by the rate at which weights can be read, then '
        'storing weights in eight bit rather than sixteen bit precision halves the bytes required '
        'per step and roughly doubles the achievable token rate at low batch sizes. The cache can '
        'be quantized as well, which raises the number of sequences that fit in a given pool. The '
        'tradeoffs are accuracy loss, which depends heavily on the quantization scheme and on '
        'which tensors are quantized, and hardware support, since the speedup only materializes '
        'if the accelerator has native arithmetic for the chosen format.\n\nSpeculative decoding '
        'attacks the sequential dependency itself. A small fast draft model proposes several '
        'tokens ahead. The large model then verifies all of the proposals in a single forward '
        'pass, which costs little more than generating one token because that pass was memory '
        'bound and had spare arithmetic capacity. Proposals that match what the large model would '
        'have produced are accepted, and the first mismatch truncates the rest. When the draft '
        'model is accurate, several tokens are emitted for the price of one, and the output '
        'distribution is provably identical to sampling from the large model alone. When it is '
        'inaccurate, the wasted work is bounded and the scheme degrades gracefully to ordinary '
        'decoding.\n\nMeasuring any of this correctly is harder than it looks, and careless '
        'measurement invalidates conclusions faster than careless implementation. Time to first '
        'token measures how long a user waits before seeing anything and is dominated by queueing '
        'and prefill. Inter token latency measures the smoothness of the stream after generation '
        'begins and is dominated by decode. Total throughput measures tokens produced per second '
        'across all concurrent requests and is what determines cost per token. These metrics '
        'trade against one another. Increasing batch size raises throughput and simultaneously '
        'raises latency for every individual request, so a single number describing a serving '
        'system is nearly meaningless. The useful artifact is a curve showing achieved throughput '
        'against tail latency as concurrency increases.\n\nSeveral measurement pitfalls recur often '
        'enough to name. Accelerator work is dispatched asynchronously, so a timer that does not '
        'explicitly synchronize with the device measures the time to enqueue work rather than the '
        'time to perform it. The first iteration after loading a model pays for kernel '
        'compilation, memory allocation, and cache warming, and including it in an average '
        'silently inflates every result. Averaging per run rates rather than dividing total work '
        'by total time overweights the fastest runs. Comparing a system against a baseline that '
        'also changed invalidates the comparison entirely. Reporting a mean without a '
        'distribution hides exactly the tail behavior that determines whether a system is usable '
        'in production.\n\nThe order in which optimizations are applied is not arbitrary, because '
        'each one exposes the next. Removing recomputation with a cache converts a compute bound '
        'quadratic loop into a memory bound linear one. Making the loop memory bound makes '
        'batching worthwhile, since batching is precisely the technique that raises arithmetic '
        'intensity. Batching many sequences creates pressure on cache memory, which makes the '
        'allocation scheme matter, which is what justifies paging. Paging raises the achievable '
        'number of resident sequences, which pushes the bottleneck toward the attention kernel '
        'and toward raw bandwidth, which is where tiling and quantization begin to pay. Building '
        'in this order means every step has a measurable justification produced by the step '
        'before it, rather than being adopted because a paper recommended it.\n\nPrefix caching '
        'generalizes block sharing into a systematic optimization. Many production workloads send '
        'requests that begin identically. A chat application prepends the same system '
        'instructions to every conversation. A retrieval system prepends the same document to a '
        'series of questions. A few shot prompt repeats the same examples across thousands of '
        'calls. Under naive serving, every one of those requests pays the full prefill cost for '
        'the shared portion, even though the resulting keys and values are bit for bit identical '
        'every time. A cache keyed on the hash of the token prefix lets a new request reuse those '
        'blocks directly, skipping the redundant computation entirely and starting its prefill at '
        'the first token that actually differs. The savings scale with how much of the prompt is '
        'shared, and in workloads with long fixed preambles the reduction in prefill work can '
        'exceed an order of magnitude.\n\nMemory pressure eventually forces a scheduler to make '
        'choices it would rather avoid. When the pool is full and a new request arrives, or when '
        'a resident sequence grows beyond what remains available, something has to give. '
        'Preemption evicts a running sequence to free its blocks, and the evicted sequence must '
        'later be resumed. Resuming can be done by recomputing its cache from the original '
        'prompt, which costs a fresh prefill but no extra memory, or by swapping the blocks out '
        'to host memory and copying them back later, which preserves the work but consumes '
        'bandwidth over the interconnect. Neither is free, and the right choice depends on '
        'sequence length, on interconnect speed, and on how long the sequence is likely to remain '
        'suspended. A scheduler that never preempts simply rejects work instead, which converts a '
        'memory problem into a queueing problem without solving anything.\n\nModel parallelism '
        'becomes necessary when the weights alone exceed the capacity of a single accelerator. '
        'Tensor parallelism splits individual matrices across devices, so every device holds a '
        'slice of every layer and the devices must communicate at multiple points within each '
        'layer to combine partial results. The communication happens on the critical path of '
        'every forward pass, which makes tensor parallelism sensitive to interconnect bandwidth '
        'and generally confines it to devices within a single machine. Pipeline parallelism '
        'instead assigns whole layers to different devices, so a request flows through them in '
        'sequence. Communication is far lighter, but a naive pipeline leaves devices idle while '
        'they wait for work from the previous stage, and filling those bubbles requires enough '
        'concurrent requests in flight to keep every stage busy. Real deployments frequently '
        'combine both, and the choice interacts with batching in ways that are easy to get '
        'wrong.\n\nAt small model sizes on fast accelerators, an entirely different bottleneck '
        'appears. If a single decode step takes only a couple of milliseconds of device time, the '
        'host side work of launching kernels, dispatching operations through the framework, and '
        'running the sampling logic in an interpreted language can consume a comparable amount of '
        'wall clock. The accelerator finishes and then waits for instructions. Captured execution '
        'graphs solve this by recording the entire sequence of kernel launches once and replaying '
        'it as a single unit, eliminating per launch overhead and interpreter time. The technique '
        'does nothing for a large model where each kernel runs long enough to hide the launch '
        'cost, but for a small model it can be the difference between forty percent utilization '
        'and ninety.\n\nSampling deserves more attention than it usually receives, because it sits '
        'inside the innermost loop. The output of the final layer is a vector of scores over the '
        'entire vocabulary, which for a modern tokenizer may contain well over a hundred thousand '
        'entries. Greedy decoding simply takes the maximum, which is cheap. Temperature scaling '
        'divides the scores before normalizing, flattening or sharpening the distribution. Top k '
        'filtering keeps only the highest scoring candidates, which requires a partial sort. '
        'Nucleus filtering keeps the smallest set of candidates whose cumulative probability '
        'exceeds a threshold, which requires a full sort and a cumulative sum across the '
        'vocabulary. That sort is not free, and at large batch sizes it can consume a visible '
        'fraction of each step. It is also numerically delicate: a cumulative sum over a hundred '
        'thousand values in reduced precision can saturate the available mantissa and silently '
        'corrupt the threshold, so the filtering arithmetic is normally promoted to higher '
        'precision even when the model itself runs in a reduced format.\n\nTokenization is the '
        'boundary between text and everything described above, and its properties leak into the '
        'system in ways that surprise people. Modern models use byte level encodings, where the '
        'atomic units are bytes rather than characters. A single token may therefore represent an '
        'incomplete portion of a multibyte character, which means decoding one token identifier '
        'in isolation can produce a replacement character rather than the intended symbol. '
        'Streaming interfaces must buffer until a complete character is available. Any code that '
        'decodes tokens individually and reassembles the resulting strings will corrupt text that '
        'falls outside the basic Latin range. The safe discipline is to work in token identifiers '
        'throughout the generation loop and to decode only when text is actually required.\n\nCost '
        'modeling ties the engineering back to economics. The meaningful figure for a served '
        'model is the price of producing a thousand tokens, which is determined by the hourly '
        'rate of the hardware divided by the sustained token rate achieved on it. That framing '
        'produces conclusions that raw benchmark numbers obscure. A faster accelerator with a '
        'higher hourly rate can be cheaper per token if its throughput advantage exceeds its '
        'price premium. A cheaper accelerator with half the memory bandwidth will produce tokens '
        'at roughly half the rate for a memory bound workload, and if its price is more than half '
        'as high it is the more expensive option despite the lower sticker rate. Utilization '
        'matters just as much as peak performance, because an accelerator that sits idle waiting '
        'for requests costs the same as one running at capacity. This is the deepest '
        'justification for continuous batching and aggressive scheduling: they convert hardware '
        'that was already paid for into output that can be sold.\n\nCorrectness has to be verified '
        'alongside performance, and it is easy to neglect. An optimization that makes a system '
        'faster while quietly changing its outputs has not been validated, it has merely been '
        'benchmarked. The strongest available check is to run the optimized implementation and a '
        'trusted reference on identical inputs under deterministic settings and compare the '
        'generated token identifiers directly. Exact equality is the ideal, but reduced precision '
        'arithmetic with differently shaped kernels will eventually diverge for reasons that have '
        'nothing to do with a bug, so the more informative measurement is the position at which '
        'the two sequences first differ. A divergence at the second token indicates a genuine '
        'defect. A divergence after two hundred tokens is ordinary numerical drift accumulating '
        'through repeated operations. Recording that position for every comparison turns'
    ),
}

PROMPT_TOKENS = {
    'p16': 16,
    'p64': 64,
    'p256': 256,
    'p1024': 1024,
    'p4096': 4096,
}
